{
  "name": "@vechain/connex-repl",
  "version": "1.1.1",
  "description": "",
  "main": "dist/index.js",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "build": "rm -rf dist/;tsc -p .",
    "lint": "tslint -p .",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "connex": "dist/index.js"
  },
  "author": "qianbin",
  "license": "LGPL-3.0",
  "dependencies": {
    "@vechain/connex-framework": "^1.1.0",
    "@vechain/connex.driver-nodejs": "^1.1.1",
    "bignumber.js": "^9.0.0",
    "repl.history": "^0.1.4"
  },
  "devDependencies": {
    "ts-node": "^8.2.0",
    "typescript": "^3.5.1"
  }
}
